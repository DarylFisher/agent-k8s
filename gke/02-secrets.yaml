apiVersion: v1
kind: Secret
metadata:
  name: scheduler-secrets
  namespace: agent-scheduler
  labels:
    app: scheduler
type: Opaque
data:
  # Base64 encoded credentials
  # IMPORTANT: Generate new passwords for production!
  #
  # To generate new base64 encoded passwords:
  #   echo -n 'your_username' | base64
  #   echo -n 'your_secure_password' | base64
  #
  # Current values (for reference - CHANGE THESE):
  #   DATABASE_USER: scheduler_user
  #   DATABASE_PASSWORD: scheduler_password

  DATABASE_USER: c2NoZWR1bGVyX3VzZXI=
  DATABASE_PASSWORD: c2NoZWR1bGVyX3Bhc3N3b3Jk
